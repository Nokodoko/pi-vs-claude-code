# Interactive-Invariant Test (Section 7) — Pending Execution

**Status:** DEFERRED — requires two live pi terminals and a human at the keyboard. The autonomous swarm cannot drive this test.

## Procedure (verbatim from spec §7)

1. Start agent A: `ca alpha --color cyan`.
2. Start agent B: `ca beta --color magenta`.
3. In agent A's REPL, invoke `coms_net_ask { target: "beta", prompt: "Count to 10 slowly." }`.
4. **Immediately** switch focus to agent B's REPL. Type: "What is 2+2?" and submit.
5. Observe:
   - Agent B responds to "What is 2+2?".
   - Agent A's `coms_net_ask` eventually returns with beta's "Count to 10" response.

## Pass criteria

- Both interactions complete without deadlock.
- No dropped messages on either side.
- B's REPL stays interactive during the auto-injection.

## Fail criteria

- "2+2" reply never appears.
- `coms_net_ask` hangs indefinitely.
- B's pi REPL freezes during the injection.

## Output capture (to be pasted verbatim by the human implementer)

> When you run this test, paste the FULL terminal output of both `ca alpha` and `ca beta` sessions here, including timestamps. This file is the spec's audit artifact for the interactive invariant; without it T11 is not signed off.

```
[ paste alpha terminal output here ]
```

```
[ paste beta terminal output here ]
```

## Result

- [ ] PASS
- [ ] FAIL — describe failure mode:

---

*Generated by the coms_auto_await swarm. Spec ref: `SPEC/coms_auto_await/coms_auto_await.md` §7, §11 T11.*
